// Correct Toy Syntax 
int why(int a, int b) {
	test = test * 100;
}

double distance(int a, int b) {
	int x;
	int y;
	test = test * y;
	x = 10 + 15;
	y = x - 90;
	x = - 90;
	y = y % 900;
	string hello;
	hello = "hello";
	return sqrt(a * b);
}

class Point implements cookie, monster {
	while (x + y > 15) {
		println("Nothing of consequence here");
	}
}